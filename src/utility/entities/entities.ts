import { Option } from './option.entity';
import { Poll } from './poll.entity';
import { User } from './user.entity';
import { Vote } from './vote.entity';

export const entities = [User, Poll, Option, Vote];
